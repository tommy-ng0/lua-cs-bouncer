# 24d without the FALLBACK_REMEDIATION line, which is the point: the fallback the
# override depends on has to come from the default in config.lua rather than from
# the file. Everything else here matches 24d.
APPSEC_URL=
ENABLED=true
API_URL=http://127.0.0.1:8081
API_KEY=LFrdL+aiecMTSxpGE9vLkx5sGMwdIpgVovpVMfXp3J0
CACHE_EXPIRATION=1
BOUNCING_ON_TYPE=all
OVERRIDE_REMEDIATION=captcha
REQUEST_TIMEOUT=3000
UPDATE_FREQUENCY=10
MODE=live
EXCLUDE_LOCATION=/v1/decisions
BAN_TEMPLATE_PATH=./ban
REDIRECT_LOCATION=
RET_CODE=
CAPTCHA_PROVIDER=not-a-provider
SECRET_KEY=
SITE_KEY=
CAPTCHA_TEMPLATE_PATH=templates/captcha.html
CAPTCHA_EXPIRATION=3600
ALTCHA_COST=100
