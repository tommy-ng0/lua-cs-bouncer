APPSEC_URL=
ENABLED=true
API_URL=http://127.0.0.1:8081
API_KEY=LFrdL+aiecMTSxpGE9vLkx5sGMwdIpgVovpVMfXp3J0
CACHE_EXPIRATION=1
# bounce for all type of remediation that the bouncer can receive from the local API
BOUNCING_ON_TYPE=all
FALLBACK_REMEDIATION=ban
REQUEST_TIMEOUT=3000
UPDATE_FREQUENCY=10
# live or stream
MODE=live
# only the LAPI stub needs excluding; /_dict is probed without an X-Forwarded-For,
# so it arrives as 127.0.0.1, which the stub answers with no decision
EXCLUDE_LOCATION=/v1/decisions,/_dict,/_cache
#those apply for "ban" action
# /!\ REDIRECT_LOCATION and RET_CODE can't be used together. REDIRECT_LOCATION take priority over RET_CODE
BAN_TEMPLATE_PATH=
REDIRECT_LOCATION=
RET_CODE=
#those apply for "captcha" action
#valid providers are recaptcha, hcaptcha, turnstile, altcha
CAPTCHA_PROVIDER=altcha
SECRET_KEY=
SITE_KEY=
CAPTCHA_TEMPLATE_PATH=templates/captcha.html
# the page under test: served to captcha decisions that arrive over plain HTTP
CAPTCHA_INSECURE_TEMPLATE_PATH=templates/captcha_insecure.html
CAPTCHA_EXPIRATION=3600
# kept low so the test suite does not spend its time deriving keys
ALTCHA_COST=100
